# Call Plane

Rust reference implementation of the gRPC transport/runtime subsystem.

Owns service registration, routing, request correlation, streaming event transport, and connection lifecycle.

It does not perform semantic planning.
