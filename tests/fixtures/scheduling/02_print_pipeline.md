Print the STL called benchy-final from my files share. First locate the newest matching STL and verify its SHA-256. In parallel, check that the printer daemon is reachable and inspect the printer's current status. After all checks pass, copy the STL to the printer staging directory, start the print, and then monitor the print until completion. Keep the file lookup and printer checks independent so they can happen concurrently.
